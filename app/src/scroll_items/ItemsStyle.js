import styled from 'styled-components/native';

export const ListaItems = styled.ScrollView.attrs({
    horizontal: true,
    showsHorizontalScrollIndicator: false,
})``;

export const Items = styled.View`
    width: 75;
    height: 100;
`;

